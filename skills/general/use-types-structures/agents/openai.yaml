interface:
  display_name: "Use Types Structures"
  short_description: "Choose repo data structures intentionally"
  default_prompt: "Use $use-types-structures when implementing or reviewing this feature so the solution checks the @batoanng/types npm package (https://www.npmjs.com/package/@batoanng/types), reuses it where appropriate, and explains the expected complexity of the critical path."
